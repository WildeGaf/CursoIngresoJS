
function mostrar()
{
var base;
var alt;
var lado;
var suma;
base = prompt("Ingrese la base");
alt = prompt("Ingrese la altura");
base = parseint(base);
alt = parseint(alt)
lado = (alt * 2);
suma = (base + lado);
sup = (base * alt / 2);
alert ("El perimetro es de "+suma"y la superficie es de "+sup);
}
